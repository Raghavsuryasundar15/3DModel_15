# 3DModel_15
With the help of three.js and drei ; a 3d model which is uploaded (.glb file); can be tranformed (position;rotation;scale) at the user end wishes. Including the gizmo property which size is predicted based upon the size of the model imported .
